# **Инструкция по использованию системы контроля версий Git**

![Картинка пример](photo_2026-01-27_13-22-12.jpg)

## Что такое Git

Программа Git это — самая популярная система контроля версий, но не единственная. Алгоритм работы подобных систем схож.

## Инициализация репозитория

Чтобы инициализировать (создать) новый репозиторий нужно ввести в терминале команду:

    git init

 ## Проверка состояния репозитория

 Чтобы проверить состояние репозитория нужно ввести команду:

    git status

## Добавление изменений в индекс

Чтобы добавить изменения в индекс для следующего коммита нужно ввести команду:

    git add <filename>

## Сохранение текущих изменений

Чтобы сохранить изменения в истории проекта нужно ввести команду:

    git commit

 ## Сохранение текущих изменений с комментарием

Чтобы сохранить изменения в истории проекта с комментарием для этих изменений нужно ввести команду:

    git commit -m "message"

## Сохранение во всех известных в проекте файлах

Чтобы сохранить все изменения, которые были в истории проекта за один раз, нужно ввести команду:
    
    git commit -a

## Сохранение во всех известных в проекте файлах с комментарием

Чтобы сохранить все изменения, которые были в истории проекта за один раз с комментарием, нужно ввести команду:

    git commit -am "message"

## Вызвать журнал изменений

Чтобы посмотреть все изменения в журнале нужно ввести комманду:

    git log

## Вызвать краткий журнал изменений

Чтобы посмотреть все изменения в журнале в кратком и читаемом формате нужно ввести комманду:

    git log --oneline

## Вызвать журнал изменений всего проекта

Чтобы посмотреть все изменения проекта, а не только текущей ветви в журнале нужно ввести комманду:

    git log --all


## Вызвать краткий журнал изменений всего проекта

Чтобы посмотреть все изменения проекта, а не только текущей ветви в журнале в кратком и читаемом формате нужно ввести комманду:

    git log --oneline --all

## Несохранённые изменения в коде

Чтобы посмотреть конкретные изменения в коде, которые вы внесли, но еще не закоммитили нужно ввести команду:
    
    git diff

## Переключение между версиями

Чтобы временно вернуться к любому конкретному сохранению в прошлом нужно ввести команду:

    git checkout <hash>

## Ветвление
